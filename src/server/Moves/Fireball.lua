local Moves = require(script.Parent)

local module: Moves.MoveFunctions = {
    COOLDOWN = 5,
    activate = function(Player: Player)
        return {}
    end,
    deactivate = function()
        
    end
}


return Moves;